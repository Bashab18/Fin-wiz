// config.js — single place to point the frontend at the backend API.
//
// Same-origin deploy (everything on one host, e.g. Render): leave this empty.
// Split deploy (static frontend on Netlify, API on Render): set this to the
// full origin of your deployed backend.
window.API_BASE_URL = 'https://fin-wiz.onrender.com';
